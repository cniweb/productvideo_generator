---
phase: 01-cli-output-fundament
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [productvideo_generator.py, tests/test_productvideo_generator.py]
autonomous: true
requirements: [CLI-01, CLI-03, CLI-04]

must_haves:
  truths:
    - "Nutzer kann Produkt/Topic per CLI-Argument oder interaktiv eingeben und der Run startet."
    - "Fehlende Pflicht-ENVs werden vor dem Run klar gemeldet und blockieren nachvollziehbar."
    - "Output-Dateien werden mit konsistenter Normalisierung benannt und im Zielverzeichnis angelegt (fehlende Output-Pfade werden auto-erzeugt)."
  artifacts:
    - path: "productvideo_generator.py"
      provides: "CLI-Input-Flow, ENV-Validierung, Normalisierung und Pfadaufbau"
      contains: "normalize_topic"
    - path: "tests/test_productvideo_generator.py"
      provides: "CLI/Output-Normalisierung und ENV-Fehlertexte werden abgedeckt"
  key_links:
    - from: "productvideo_generator.py"
      to: "OUTPUT_DIR"
      via: "os.path.join"
      pattern: "os\.path\.join\(OUTPUT_DIR"
    - from: "productvideo_generator.py"
      to: "normalize_topic"
      via: "filename construction"
      pattern: "normalize_topic\(self\.topic\)"
---

<objective>
Ein zentraler CLI-Input-Flow und stabile Dateinamen sorgen fuer einen verlasslichen Start und konsistente Outputs.

Purpose: Phase-1 Grundlagen fuer Input/Config/Output verankern, bevor externe Services laufen.
Output: Aktualisierte CLI-Eingabe/ENV-Validierung, konsistente Dateinamen, Tests fuer Normalisierung und Fehlertexte.
</objective>

<execution_context>
@C:/Users/Admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-output-fundament/01-CONTEXT.md
@.planning/phases/01-cli-output-fundament/01-RESEARCH.md
@productvideo_generator.py
@tests/test_productvideo_generator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI-Input-Flow mit Validierung und Defaults</name>
  <files>productvideo_generator.py</files>
  <action>
    Ersetze den direkten input()-Aufruf im __main__ durch eine zentrale Input-Funktion, die den Prioritaets-Flow umsetzt: CLI-Argument > stdin (Pipe) > interaktiver Prompt.
    - Verwende argparse (stdlib) fuer optionales Topic-Argument, ohne neue Dependencies.
    - Lies stdin nur, wenn es nicht interaktiv ist; sonst interaktiver Prompt mit Beispiel-Topic.
    - Blockiere leeren oder zu kurzen Input mit klarer Fehlermeldung (Kurz + naechste Aktion). Entscheide eine Minimallaenge (Empfehlung: 3 Zeichen) und nenne sie im Prompt/Fehlertext.
    - Wenn Defaults fuer fehlende optionalen ENV/Flags genutzt werden (z. B. VIDEO_MAX_SECONDS, VIDEO_ASPECT_RATIO, VIDEO_RESOLUTION), gib sie beim Start als kurze Info aus.
    - Behalte den bestehenden Run-Flow (Trends -> Skript -> Video -> Metadaten) unveraendert; nur Input-Prioritaet und Validierung aendern.
    - Fehlertexte fuer Input muessen dem Format aus CONTEXT.md folgen: kurz + naechste Aktion.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_productvideo_generator.py::test_generate_sales_script_writes_file</automated>
    <manual>Optional: ./run.sh ohne Argument startet Prompt, mit Argument ueberspringt Prompt.</manual>
    <sampling_rate>run once after task completion</sampling_rate>
  </verify>
  <done>CLI-Argument, stdin oder Prompt liefern ein validiertes Topic; leer/zu kurz wird blockiert mit klarer Aktion.</done>
</task>

<task type="auto">
  <name>Task 2: Einheitliche Normalisierung fuer Output-Dateinamen</name>
  <files>productvideo_generator.py, tests/test_productvideo_generator.py</files>
  <action>
    Fuehre eine zentrale normalize_topic()-Funktion ein (stdlib: re/unicodedata) und nutze sie fuer alle Output-Dateien.
    - Normalisiere: trim, collapse whitespace, ersetze nicht erlaubte Zeichen durch "_", entferne doppelte/leading/trailing Underscores.
    - Definiere Mindestlaenge fuer Dateinamen (z. B. "topic"-Fallback) falls nach Normalisierung zu kurz/leer.
    - Verwende normalize_topic(self.topic) fuer Skript-, Video- und Meta-Dateinamen sowie im Meta-JSON script_file/video_file.
    - Entferne direkte self.topic.replace(' ', '_') Verwendungen zugunsten der zentralen Funktion.
    - Stelle sicher, dass OUTPUT_DIR existiert (bei Bedarf auto-erstellen), bevor Dateien geschrieben werden; nutze os.makedirs(..., exist_ok=True).
    - Ergänze Tests fuer Normalisierung in tests/test_productvideo_generator.py (z. B. Sonderzeichen, Mehrfach-Leerraum, sehr kurzer Input).
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_productvideo_generator.py</automated>
    <manual>Optional: pruefe, dass Output-Dateien fuer Topics mit Sonderzeichen stabil geschrieben werden.</manual>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>Alle Outputs nutzen denselben normalisierten Dateinamen; fehlende Output-Pfade werden automatisch erstellt; Tests decken Normalisierungs-Edge-Cases ab.</done>
</task>

<task type="auto">
  <name>Task 3: Klarere ENV-Fehlermeldungen und Fail-fast-Check</name>
  <files>productvideo_generator.py, tests/test_productvideo_generator.py</files>
  <action>
    Schärfe _check_env_file/_raise_env_error so, dass fehlende Pflicht-ENVs klar gelistet werden (bereits vorhanden) und die Fehlermeldung eine konkrete naechste Aktion enthaelt.
    - Stelle sicher, dass der Run bei fehlenden ENVs vor jeglicher Verarbeitung abbricht (Fail fast).
    - Formatiere die Fehlermeldung kurz, mit einem Schritt zur Behebung (z. B. "Erstelle .env aus .env.example und setze: ...").
    - Ergänze einen Testfall, der fehlende ENV-Keys provoziert und die Fehlermeldung auf fehlende Variablen + naechsten Schritt prueft.
  </action>
  <verify>
    <automated>python -m pytest -q tests/test_productvideo_generator.py</automated>
    <manual>Optional: python productvideo_generator.py ohne .env zeigt klare Fehlermeldung und beendet den Run.</manual>
    <sampling_rate>run after task completion</sampling_rate>
  </verify>
  <done>ENV-Fehler brechen vor Run-Start ab und kommunizieren fehlende Keys + naechsten Schritt.</done>
</task>

</tasks>

<verification>
python -m pytest -q tests/test_productvideo_generator.py
</verification>

<success_criteria>
  - Input-Flow priorisiert CLI-Argument > stdin > Prompt und blockiert zu kurze Eingaben.
  - Fehlende Pflicht-ENVs werden vor Run-Start klar gemeldet (inkl. fehlender Keys und naechster Aktion).
  - Alle Output-Dateien nutzen konsistente Normalisierung ueber eine zentrale Funktion und Output-Verzeichnisse werden bei Bedarf angelegt.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-output-fundament/01-01-SUMMARY.md`
</output>
